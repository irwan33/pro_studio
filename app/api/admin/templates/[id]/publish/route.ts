import { prisma } from "@/lib/db"; import { requireAdmin } from "@/lib/auth/session"; import { handleApiError, ok } from "@/lib/api/response"; export async function PATCH(req: Request, { params }: { params: Promise<{ id: string }> }) { try { await requireAdmin(); const body = await req.json(); return ok(await prisma.template.update({ where: { id: (await params).id }, data: { isPublished: Boolean(body.isPublished) } })); } catch (e) { return handleApiError(e); } }